# Release 0.9.4 — External Audit Anchor

- âncora externa do HEAD hash da cadeia de decisão;
- provider `FILE_APPEND_ONLY` com arquivo criado sem sobrescrita;
- provider `MANUAL_EXTERNAL` para referência externa controlada;
- verificação cadeia × ponto ancorado × recibo externo;
- eventos `ANCHOR_PUBLISHED` e `ANCHOR_VERIFIED`;
- API, UI, Admin read-only e management command;
- pacotes de evidência passam a incluir `external_anchors.json`;
- configuração `MPS_AUDIT_ANCHOR_DIR`;
- versão corrigida em `config/settings.py` para 0.9.4.
