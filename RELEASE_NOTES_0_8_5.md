# Release 0.8.5 — Budget e cash-flow temporal

- `Supplier.payment_terms_days` e `payment_terms_code`.
- `MPSFinancialBudget` e `MPSFinancialBudgetLine`.
- `MPSRevisionSimulationCashFlowBucket`.
- PURCHASE temporalizado em release + prazo de pagamento.
- LABOR/MACHINE/OVERHEAD temporalizados na data planejada MAKE.
- INVENTORY_VALUE por fim de bucket.
- Comparação budget × baseline × revisão e variância.
- Integração automática ao MRP what-if 0.8.3/0.8.4.
- API, Admin, CLI e relatório HTML ampliado.

Os valores são proxies de planejamento; não substituem contas a pagar, tesouraria ou contabilidade.
